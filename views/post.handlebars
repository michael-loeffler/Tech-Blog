<h1>{{post.title}}</h1>
<h3>Posted by {{post.user.username}} on {{format_date post.date_created}}</h3>
<p>{{post.content}} </p>

{{#each post.comments as |comment|}}
<p>{{comment.content}} - <i>{{comment.user.username}}</i></p>
{{/each}}
{{#if logged_in}}
<button>Leave a comment</button>
{{/if}}

